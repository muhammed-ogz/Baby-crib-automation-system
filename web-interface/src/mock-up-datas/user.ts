import type { User } from "../types/data";

// Kullanıcı mock verisi
export const mockUser: User = {
  id: "user-001",
  name: "admin",
  email: "admin@example.com",
  avatar: undefined, // Avatar yoksa default bir ikon gösterilecek
};
